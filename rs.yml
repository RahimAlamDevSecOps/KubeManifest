kind: ReplicaSet
apiVersion: apps/v1
metadata:
  name: myreplicaset

spec:
  replicas: 2
  selector:
    matchLabels:
      myname: rahim

  template:
    metadata:
      labels:
        myname: rahim
    
    spec:
      containers:
        - name: c00
          image: ubuntu
          command: ["/bin/bash", "-c", "while true; do echo Hello-Rahim; sleep 5; done"]
